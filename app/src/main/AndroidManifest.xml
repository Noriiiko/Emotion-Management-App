<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.emomanager">
    <!-- 定位所需权限 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--android:roundIcon="@mipmap/ic_launcher_round" -->
    <!--android:icon="@mipmap/ic_launcher"-->
    <!--singleinstance：从别的app跳转回来打开还是原来的页面-->
    <application
        android:allowBackup="true"
        android:launchMode="singleInstance"
        android:icon="@drawable/logo1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Emomanager">
        <activity
            android:name=".CalendarSample"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--record feelings-->
        <activity
            android:name=".RecordFeelings"
            android:exported="true" >


        </activity>
    <!-- 百度地图开发者AK认证 -->
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="Ob9fBtxwoYQzUvhLZ9k8jauBMYd6UZGR" />

        <activity
            android:name=".DiaryEditorActivity"
            android:exported="true"/><!--android:label="Diary"--><!-- 暂定 -->

        <!-- 百度地图配置 -->
        <service
            android:name="com.baidu.location.f"
            android:enabled="true"
            android:process=":remote"/>

        <activity
            android:name=".AnalyzeRepo"
            android:exported="true">

        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>